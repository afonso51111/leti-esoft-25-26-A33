# US 13 - Register a Vaccination Center

## 1. Requirements Engineering

### 1.1. User Story Description

As DGS Administrator, I want to register a vaccination center.

### 1.2. Customer Specifications and Clarifications

**From the specifications document:**

> The system must allow the registration of different types of vaccination centers, such as Healthcare Centers and Community Mass Vaccination Centers.
> **Sprint 3 Requirement:** Community Mass Vaccination Centers must include the vaccine type administered.

**From the client clarifications:**

> **Question:** Does the vaccination center have an ID?
>
> **Answer:** Vaccination centers do not have an identifier (ID). During the center creation process, a name must be entered, which must be unique relative to all registered centers.
>
> **Question:** Is there a fixed opening and closing time?
>
> **Answer:** Each center autonomously establishes its opening and closing hours.

### 1.3. Acceptance Criteria

- **AC13-1:** Community Mass Vaccination Centers must include the vaccine type administered (e.g., Covid-19).
- **AC13-2:** The vaccination center name must be unique within the system.
- **AC13-3:** The vaccination center must be classified as either a Healthcare Center or a Community Mass Vaccination Center.
- **AC13-4:** Required fields (name, address, phone number, email, website, opening/closing hours, slot duration, max vaccines per slot) cannot be empty.
- **AC13-5:** If the center is a Healthcare Center, ARS and AGES must be specified.

### 1.4. Found out Dependencies

- **US10 (Specify Vaccine Type):** Dependent on this US because Community Mass Vaccination Centers require a valid Vaccine Type to be selected.

### 1.5 Input and Output Data

**Input Data:**

- Typed data:
  - name
  - postal address
  - phone number
  - email address
  - website address
  - opening hours
  - closing hours
  - slot duration
  - maximum number of vaccines per slot
  - ARS (only for Healthcare Center)
  - AGES (only for Healthcare Center)

- Selected data:
  - Type of Vaccination Center (Healthcare / Community)
  - **Vaccine Type (only for Community Mass Vaccination Center)**

**Output Data:**

- (In)success of the operation

### 1.6. System Sequence Diagram (SSD)

![US13-SSD](svg/US13-SSD.svg)

### 1.7 Other Relevant Remarks

- The creation of a Vaccination Center implies the existence of physical resources such as a Waiting Room and a Recovery Room (Composition relationship).


## 2. OO Analysis

### 2.1. Relevant Domain Model Excerpt

![US13-MD](svg/US13-MD.svg)

### 2.2. Other Remarks

- Inheritance pattern: Healthcare Center and Community Mass Vaccination Center inherit from abstract class Vaccination Center.
- **Association:** Community Mass Vaccination Center has a relationship with **Vaccine Type**.
- Composition: A Vaccination Center has a Waiting Room and a Recovery Room.

## 3. Design - User Story Realization

### 3.1. Rationale

| Interaction ID | Question: Which class is responsible for... | Answer | Justification (with patterns) |
|:--- |:--- |:--- |:--- |
| Step 1 | ... interacting with the actor? | RegisterVaccinationCenterUI | Pure Fabrication: responsible for user interaction (View). |
| | ... coordinating the US? | RegisterVaccinationCenterController | Controller: coordinates the use case logic. |
| Step 2 | ... knowing the available center types? | RegisterVaccinationCenterController | Controller: provides supported subclasses. |
| | ... requesting the center type? | RegisterVaccinationCenterUI | IE: responsible for user interaction. |
| **Step 3 (New)** | ... knowing the available vaccine types? | **VaccineTypeStore** | **IE**: Owns the list of vaccine types. |
| | ... retrieving vaccine types (if Community)? | RegisterVaccinationCenterController | Controller: Mediates between UI and VaccineTypeStore (via Company). |
| Step 4 | ... requesting the common data? | RegisterVaccinationCenterUI | IE: responsible for user interaction. |
| Step 5 | ... saving the inputted data? | RegisterVaccinationCenterUI | IE: temporarily keeps the data inputted by the user. |
| Step 6 | ... instantiating a new vaccination center? | VaccinationCenterStore | Creator/Pure Fabrication: By applying HC + LC, `Company` delegates to a dedicated Store. |
| | ... knowing the VaccinationCenterStore? | Company | IE: The Company is the root entity that manages domain containers. |
| | ... creating the specific object? | VaccinationCenterStore | **Factory/Creator**: Creates `HealthcareCenter` (with ARS/AGES) or `CommunityMassVaccinationCenter` (with **VaccineType**) based on selection. |
| | ... validating the data (local validation)? | Vaccination Center (and subclasses) | IE: The object itself validates its own attributes. |
| | ... validating the data (global validation)? | VaccinationCenterStore | IE: Knows all existing centers and can check for duplicates (AC13-2). |
| Step 7 | ... saving the created center? | VaccinationCenterStore | IE: Owns the collection/list of vaccination centers. |
| Step 8 | ... informing operation success? | RegisterVaccinationCenterUI | IE: responsible for user interaction. |

### Systematization

According to the taken rationale, the conceptual classes promoted to software classes are:

- Company
- Vaccination Center (abstract)
- Healthcare Center
- Community Mass Vaccination Center
- **Vaccine Type**

Other software classes (i.e. Pure Fabrication) identified:

- RegisterVaccinationCenterUI
- RegisterVaccinationCenterController
- VaccinationCenterStore
- **VaccineTypeStore**

### 3.2. Sequence Diagram (SD)

![US13-SD](svg/US13-SD.svg)

### 3.3. Class Diagram (CD)

![US13-CD](svg/US13-CD.svg)

## 4. Tests

**Test 1:** Check that it is possible to create an instance of Healthcare Center with valid values.

    TEST_F(HealthcareCenterFixture, CreateValidHealthcareCenter){
        EXPECT_NO_THROW(new HealthcareCenter(L"Centro Porto", ..., L"ARS Norte", L"Agrupamento X"));
    }

**Test 2:** Check that it is possible to create a Community Center with a valid Vaccine Type.

    TEST_F(CommunityCenterFixture, CreateValidCommunityCenter){
        // Assumes 'vaccineType' is a valid object from fixture
        EXPECT_NO_THROW(new CommunityMassVaccinationCenter(L"Centro Matosinhos", ..., this->vaccineType));
    }

**Test 3:** Check that the store rejects saving a center with a duplicate name (validates AC13-2).

    TEST_F(VaccinationCenterStoreFixture, FailToSaveDuplicateCenter){
        shared_ptr<VaccinationCenter> c1 = this->store->createHealthcareCenter(L"Centro Lisboa", ...);
        this->store->save(c1);

        // Try to create another center with the same name
        shared_ptr<VaccinationCenter> c2 = this->store->createCommunityCenter(L"Centro Lisboa", ..., this->vaccineType);

        EXPECT_THROW(this->store->validate(c2), std::runtime_error); 
    }

## 5. Construction (Implementation)

- n/a

## 6. Integration and Demo

A menu option on the Administrator's console menu was added.

    int AdminMenuView::processMenuOption(int option) {
        case 1: 
            view = new RegisterVaccinationCenterUI();
            view->show();
            break;
    }

## 7. Observations

- The system now enforces that every Community Mass Vaccination Center is associated with a specific Vaccine Type, as per Sprint 3 requirements.